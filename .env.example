# Base URL prefixed to asset pointer paths (/__l5e/assets-v1/...).
# Leave empty for same-origin (default). Set to your Lovable domain or a
# custom CDN to serve assets from there in any environment.
# VITE_ASSET_BASE_URL=https://impact-connect-guild.lovable.app