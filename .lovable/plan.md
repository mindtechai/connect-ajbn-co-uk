Update the public Direct Member Messaging section sub-headline in `src/components/landing/DirectMessagingPublicSection.tsx`.

Current text (line 47):
"Connect directly and securely with fellow members, property developers, and finance providers without exposing your personal contact details."

New text:
"Connect directly and securely with fellow members, without exposing your personal contact details"

No other changes to layout, styling, or routing. After the edit, run `bun run build` to confirm the project compiles cleanly.